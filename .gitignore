# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.idea
/stackit-secrets-manager

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/
.DS_Store

*obsidian/

# ignore bin directory where we download dependencies on demand
bin/

# ignore the tmp directory where we create reports and temporary output
tmp/

# ignore the dist directory which goreleaser is using for generating the releases
dist/
